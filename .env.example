# ============================================
# PrivatePay Monad — Environment Variables
# Copy this file to .env and fill in your values.
# ============================================

# App
VITE_APP_ENVIRONMENT=dev
VITE_WEBSITE_HOST=privatepay.monad

# Backend (local relayer, optional)
VITE_BACKEND_URL=http://localhost:3400

# WalletConnect (ConnectKit) — get a project ID at https://cloud.walletconnect.com
VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id

# Supabase — from your project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Treasury — contract or EOA that receives payments (see contracts/README.md)
# After deploy: set this to the PrivatePayTreasury contract address
VITE_MONAD_TREASURY_ADDRESS=0x...

# Relayer key: used by backend to call treasury.withdraw(to, amount). Same key as RELAYER_ADDRESS at deploy.
VITE_TREASURY_PRIVATE_KEY=your_hex_private_key_no_0x

# Contract deploy (optional — only for npm run contract:deploy)
# DEPLOYER_PRIVATE_KEY=hex_key_for_deployer
# RELAYER_ADDRESS=0x...  # EOA that may call withdraw(); can be same as deployer

# Monad Testnet
VITE_MONAD_RPC_URL=https://testnet-rpc.monad.xyz/
VITE_MONAD_CHAIN_ID=10143
VITE_MONAD_EXPLORER=https://testnet.monadexplorer.com
