# Backend / Relayer (withdraw from treasury)
# Use 3400 to match VITE_BACKEND_URL in frontend .env

PORT=3400
HOST=0.0.0.0

# Treasury key (for POST /withdraw – relayer signs transfer to recipient)
# Optional: set when implementing withdraw; keep secret
# TREASURY_PRIVATE_KEY=APrivateKey1zkp...

# Supabase (to verify user balance before withdraw)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
